{
  "name": "@team-monite/sdk-themes",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "module": "dist/index.js",
  "scripts": {
    "dev": "yarn clean && tsc --watch",
    "build": "yarn clean && tsc",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@mui/material": "~5.14.2"
  },
  "devDependencies": {
    "rimraf": "~5.0.5",
    "typescript": "~5.4.5"
  },
  "files": [
    "dist",
    "src"
  ]
}
