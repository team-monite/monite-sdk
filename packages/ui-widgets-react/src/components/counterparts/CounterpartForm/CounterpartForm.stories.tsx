import Form from './CounterpartForm';

const Story = {
  title: 'In Progress/Counterparts — Form Create',
  component: Form,
};
export default Story;

export const DefaultForm = () => (
  <div style={{ maxWidth: 500 }}>
    <Form />
  </div>
);
