import ConfirmDeleteModal from './ConfirmDeleteModal';
import { Canvas, Meta, Story } from '@storybook/addon-docs';

<Meta
  title="Tags/Tags — Confirm Delete Modal"
  component={ConfirmDeleteModal}
  args={{
    tag: {
      id: 'tag-1',
      name: 'Tag Name',
    },
  }}
/>

export const Template = (args) => <ConfirmDeleteModal {...args} />;
export const WithError = (args) => <ConfirmDeleteModal {...args} tag={{
  id: '0',
  name: 'Tag with error'
}} />;

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
  <Story name="WithError">{WithError.bind({})}</Story>
</Canvas>
