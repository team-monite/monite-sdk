import TagFormModal from './TagFormModal';
import { Canvas, Meta, Story } from '@storybook/addon-docs';

<Meta
  title="Tags/Tags — Create, Update Tag Modal"
  component={TagFormModal}
/>

export const Template = (args) => <TagFormModal {...args} />;

<Canvas>
  <Story name="Create tag">{Template.bind({})}</Story>
  <Story name="Update tag" args={{
    tag: {id: 'tag-1', name: 'Existing tag'}
  }}>
    {Template.bind({})}
  </Story>
</Canvas>
