export type ThemeConfig = {
  colors?: {
    primary?: string;
    secondary?: string;
    neutral?: string;

    background?: string;

    text?: string;
  };

  typography?: {
    fontFamily?: string;
  };
};
